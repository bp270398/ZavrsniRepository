import { FilterableRouteDirective } from './filterable-route.directive';

describe('FilterableRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterableRouteDirective();
    expect(directive).toBeTruthy();
  });
});
